{
  "name": "mlc-bootstrap",
  "version": "0.0.1",
  "dependencies": {
    "elm": "^0.18.0"
  },
  "scripts": {
    "build": "elm-make src/Bootstrap.elm --output=/tmp/mlc.js",
    "build-sed": "elm-make src/MLC/Test.elm --output=/tmp/mlc.js",
    "format": "elm-format --yes src/"

  }
}
